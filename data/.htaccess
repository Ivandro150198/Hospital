# data/ não deve ser listável; JSON pode ser sensível a edição
Options -Indexes
<Files "*.json">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order deny,allow
        Deny from all
    </IfModule>
</Files>
